name = "palettelab"
type = "javascript"
compatibility_date = "2026-08-27"

# Cloudflare Pages — no Workers needed for static. This file is for `wrangler pages deploy` only.
# Deploy: npm run build && npx wrangler pages deploy dist --project-name=palettelab

[build]
command = "npm run build"
publish = "dist"
